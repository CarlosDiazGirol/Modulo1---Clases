import styles from './Footer.module.css';

function Footer() {
  return (
    <footer className={styles.footer}>
      <p>React, estado y routing en una misma SPA.</p>
    </footer>
  );
}

export default Footer;

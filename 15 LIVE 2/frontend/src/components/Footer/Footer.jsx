import styles from './Footer.module.css';

function Footer() {
  return (
    <footer className={styles.footer}>
      <p>Redux Toolkit + JWT + backend real.</p>
    </footer>
  );
}

export default Footer;

import styles from './Footer.module.css';

function Footer() {
  return (
    <footer className={styles.footer}>
      <p>Frontend React conectado a un backend real.</p>
    </footer>
  );
}

export default Footer;

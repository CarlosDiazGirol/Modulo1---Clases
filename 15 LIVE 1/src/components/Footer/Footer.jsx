import styles from './Footer.module.css';

function Footer() {
  return (
    <footer className={styles.footer}>
      <p>Estado global para usuario y carrito.</p>
    </footer>
  );
}

export default Footer;
